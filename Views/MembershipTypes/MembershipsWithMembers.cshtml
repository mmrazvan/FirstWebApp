@model FirstMVCApp.ViewModels.MembershipTypeViewModel

@{
    ViewData["Title"] = "MembershipsWithMembers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>MembershipsWithMembers</h1>

<div>
    <h4>MembershipTypeViewModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.MembershipType)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MembershipType)
        </dd>        
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
<label asp-for="Members" class="control-label">Members for this membership</label>
@foreach (var item in Model.Members)
{
    <div style="border: 1px solid blue;">
        <p>
            @Html.DisplayFor(modelItem => item.Name)
        </p>
    </div>
}

